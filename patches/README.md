# Patches Sunshine

Patches `git` appliqués sur le checkout de brave-core par
`scripts/apply_branding.py` (dans l'ordre alphabétique).

Format : fichiers `NNN-description.patch` générés avec
`git -C build/brave-browser/src/brave diff > patches/NNN-description.patch`.

Aucun patch pour l'instant (v1 : rebranding par script uniquement).
